const express = require("express");
// const { Router } = require('express');
const router = express.Router();

const TimeseriesController = require("../app/controllers/TimeseriesController");

// router.get("/faults-per-minute", TimeseriesController);

module.exports = router;
